package com.yusuf.learning;

/**
 * (1) java.security.cert.Certificate) represents a cryptographic identity certificate.
 * (2) A Java Certificate class instance contains name plus other details of the entity it identifies, plus possibly a
 *     digital signature from a Certificate Authority (CA).
 * (3) Certificate class is an abstract class, so you will always point to a subclass of Certificate.
 * (4) Certificate class has one subclass - the X509Certificate class.
 *     This class represents an X.509 certificate which is used as identity certificate in HTTPS and TLS
 * (5) We can obtain Certificate using CertificateFactory or from a KeyStore
 */

public class CertificateClient {

    public static void main(String[] args) {

    }

}
